# โครงสร้างระบบเว็บ Softwar TH (หลังแยกไฟล์)

> เป้าหมาย: index.html เบา โหลดเร็วบนเน็ตช้า แต่การอ่านยังไหลลื่นต่อเนื่องเหมือนไฟล์เดียว

## ผลลัพธ์
- `index.html` เดิม **3,102 KB → 57 KB** (เล็กลง ~54 เท่า)
- เนื้อหาถูกย้ายไป `content/*.html` โหลดเฉพาะตอนใกล้ถึง (lazy-load) + prefetch ล่วงหน้า
- บน GitHub Pages ยังได้ gzip อัตโนมัติซ้อนไปอีก (ข้อความไทยบีบ ~4–5 เท่า)

## แผนผังไฟล์
```
index.html            ← โครงเครื่อง: CSS + เมนู + hero + JS + loader (57 KB)
index-full.html       ← สำเนาไฟล์เดียวเดิม (ไว้ rollback อย่างเดียว ไม่ต้องเสิร์ฟ)
content/
  frontmatter.html    ← ภาพรวม, บทคัดย่อ, กิตติกรรมประกาศ, ประวัติ, คำย่อ, บทสรุปผู้บริหาร
  chapter1.html … chapter3.html
  ch4-1.html … ch4-12.html   ← บท 4 ซอยเป็น 12 ตอน (เพราะบท 4 หนัก 1.9 MB)
  chapter5.html, chapter6.html   ← โครงบท (ยังไม่แปล)
  bibliography.html
images/               ← รูป (เดิม) ใส่ loading="lazy" ให้ในเนื้อหาแล้ว
MD/                   ← ต้นฉบับ markdown (ไม่ถูกเสิร์ฟ ใช้อ้างอิงตอนแก้)
```

## ทำงานยังไง (สรุปสั้น)
1. เปิดหน้า → โหลดแค่ shell + 2 ส่วนแรก (frontmatter + บท 1) → เห็นเนื้อหาเร็ว
2. เลื่อนลง → `IntersectionObserver` โหลดบทถัดไปก่อนถึงจริง 1400px (ไม่สะดุด)
3. ตอน browser ว่าง → ค่อย ๆ preload ส่วนที่เหลือ ให้เลื่อนอ่านรวดเดียวได้เหมือนไฟล์เดียว
4. คลิกเมนู/ลิงก์ `#...` → โหลดเฉพาะบทที่ต้องใช้ แล้วเลื่อนไปหา (รองรับ back/forward)

โครง DOM ของบท/ตอน ถูกวาง "ช่องว่าง" (`.frag-mount`) ไว้ตามลำดับล่วงหน้า จึงรับประกันว่าเนื้อหาเรียงถูกเสมอ แม้จะโหลดสลับลำดับกัน

## วิธีแก้/เพิ่มเนื้อหาในอนาคต

**แก้เนื้อหาบทที่มีอยู่** — เปิดไฟล์ใน `content/` ตรง ๆ เช่นแก้บท 3 ก็แก้ `content/chapter3.html` ได้เลย ไม่ต้องยุ่งกับ index.html

**แปลบท 5 เสร็จ** — วางเนื้อหา HTML ลงใน `content/chapter5.html` (คงโครง `<article class="chapter-section" id="chapter5">…</article>` ไว้) เมนูและ loader ทำงานต่อได้ทันที

**เพิ่มหัวข้อย่อยใหม่ที่มี id ใหม่** — ถ้าจะให้ลิงก์ `#id-ใหม่` กระโดดตรงได้ ให้เพิ่ม id นั้นในแอตทริบิวต์ `data-ids="…"` ของ `.frag-mount` ที่เกี่ยวข้องใน index.html ด้วย (ถ้าไม่เพิ่ม ก็ยังอ่านได้ปกติด้วยการเลื่อน แค่กระโดดตรงผ่าน # ครั้งแรกจะช้ากว่า)

**ปรับสมดุลเน็ต** — อยากประหยัดแบนด์วิดท์สุด ๆ (โหลดเฉพาะที่อ่านจริง ไม่ preload พื้นหลัง) ให้ลบบรรทัด `idlePreload();` ในบล็อก LAZY CONTENT LOADER ท้าย index.html ออก

## ทดสอบบนเครื่อง (ต้องผ่านเว็บเซิร์ฟเวอร์ — เปิดไฟล์ตรง ๆ fetch จะไม่ทำงาน)
```
cd "Softwar TH - Webpage"
python3 -m http.server 8000
# เปิดเบราว์เซอร์ไปที่ http://localhost:8000
```

## Deploy ขึ้น GitHub Pages
push ทั้งโฟลเดอร์ (index.html + content/ + images/) แล้วเปิด Pages ที่ branch หลัก — gzip เปิดอัตโนมัติ ไม่ต้องตั้งค่าเพิ่ม

## Rollback
ถ้าต้องการกลับไปไฟล์เดียว: `cp index-full.html index.html`
